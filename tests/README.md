# How to create a test with pytest
pytest requires a `src/` (could be a different name) folder and a `tests/` (this name only) folder. 
File names must be in the form `test_<filename_where_function_is_located>.py` and test functions must be named like `test_<name_of_function>.py`.