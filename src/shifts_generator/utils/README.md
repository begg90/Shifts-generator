This folder contains all sorts of functions that do not belong to the solver or the domain.
There will be a need for functions about date and time that do not belong to the solver nor 
to the domain but are probably needed for both. Can probably be used also for loading data (for now CSV and JSON).
For example
- parsing of date and times
- calculating time intervals
-checking for overlapping shifts
- conversions. eg string <--> datetime
